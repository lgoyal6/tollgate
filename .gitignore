bin/
loadtest/results/
bench/.tmp/
__pycache__/
deploy/terraform/.terraform/
deploy/terraform/.terraform.lock.hcl
deploy/terraform/terraform.tfstate*
*.test
.DS_Store

# graphify knowledge graph (local index, not source)
graphify-out/

# Same reasoning as .dockerignore: the provider credential belongs in the
# process environment, never in a file next to the code.
.env
.env.*
*.pem
*.key
.schemathesis/
